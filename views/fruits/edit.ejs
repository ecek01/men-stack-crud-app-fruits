<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Fruit</title>
</head>
<body>
  <h1>Edit <%= fruit.name %></h1>
  <form action="/fruits/<%=fruit._id%>?_method=PUT" method="POST">
    <!-- Prefill the name input with the fruit's current name -->
    <label for="name">Name:</label>
    <input type="text" name="name" value="<%= fruit.name %>" required>

    <!-- Conditionally check the checkbox based on the fruit's isReadyToEat property -->
    <label for="isReadyToEat">Ready to Eat?</label>
    <input type="checkbox" name="isReadyToEat" <%= fruit.isReadyToEat ? "checked" : "" %> />

    <button type="submit">Update Fruit</button>
  </form>
  <a href="/fruits">Back to Fruits</a>
</body>
</html>